<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamnova.nova">
    <!-- 인터넷 연결 권한 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- camera, gallery -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="ChatClient.ChatRoomListActivity"></activity>
        <activity android:name="Timer.TimerActivity" />
        <activity android:name="Timer.Test01Activity" /> <!-- QR코드 스캔 액티비티 -->
        <activity
            android:name="Seat.QRCodeScanActivity"
            android:screenOrientation="portrait" /> <!-- 좌석 예약 홈 ( 메인 ) -->
        <activity
            android:name="Seat.SeatHomeActivity"
            android:screenOrientation="portrait" /> <!-- 앱시작시 스플래쉬(로딩) 액티비티 -->
        <activity
            android:name="Loading.LoadingActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 회원가입 액티비티 -->
        <activity
            android:name="Register.RegisterActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" /> <!-- 메일 인증 액티비티 -->
        <activity
            android:name="Register.CertMailActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize" /> <!-- 로그인 액티비티 -->
        <activity
            android:name="Login.LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize" /> <!-- 홈(메인) 액티비티 -->
        <activity
            android:name="Main.HomeActivity"
            android:screenOrientation="portrait" /> <!-- 프로필 페이지 -->
        <activity
            android:name="Profile.ProfileMainActivity"
            android:screenOrientation="portrait" /> <!-- 프로필 수정 -->
        <activity
            android:name="ChatClient.ChatRoomMainActivity"
            android:screenOrientation="portrait" /> <!-- 채팅 테스트 -->
        <activity
            android:name="Profile.ProfileFgModifyActivity"
            android:screenOrientation="portrait" /> <!-- 프로필 배경 수정 -->
        <activity
            android:name="Profile.ProfileBgModifyActivity"
            android:screenOrientation="portrait" /> <!-- 사진 촬영을 위한 provide -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.teamnova.inma06.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>